---
title: "Playground — ทดสอบ component"
description: "หน้าทดสอบ component ทุกตัวของ Rakthan ไม่ใช่เนื้อหาบทเรียน"
unlisted: true
---

import BinaryConverter from '@site/src/components/BinaryConverter';
import Callout from '@site/src/components/Callout';
import Diagram from '@site/src/components/Diagram';
import PythonRunner from '@site/src/components/PythonRunner';
import Quiz from '@site/src/components/Quiz';

# Playground

หน้านี้ใช้ทดสอบ component ทุกตัวก่อนเอาไปใช้ในบทเรียนจริง
ไม่อยู่ในสารบัญและไม่ถูกเครื่องมือค้นหาเก็บข้อมูล

:::warning ไม่ใช่เนื้อหาบทเรียน

ถ้าคุณมาถึงหน้านี้จากการค้นหา ให้กลับไปที่ [หน้าเริ่มต้น](/docs/intro)

:::

## `<Callout>`

ครบทั้ง 6 type แต่ละอันมีไอคอนต่างกัน — ลองปิดสีในหัวจอแล้วดูว่ายังแยกออกไหม

<Callout type="misconception">
  เพิ่มบิตทีละ 1 ตัว จำนวนค่าที่เก็บได้จะเพิ่มขึ้น**ทีละสองเท่า** ไม่ใช่เพิ่มทีละสอง

  8 บิตเก็บได้ 256 ค่า ส่วน 9 บิตเก็บได้ 512 ค่า ไม่ใช่ 258
</Callout>

<Callout type="misconception" title="คนมักเข้าใจผิดว่า ตัวแปรคือกล่อง">
  ทดสอบการใส่ `title` เอง แทนหัวเรื่องเริ่มต้น
</Callout>

<Callout type="note">
  ตัวเลข 0.1 + 0.2 ในภาษา Python ให้ผลเป็น 0.30000000000000004 ไม่ใช่ 0.3
</Callout>

<Callout type="tip">
  ถ้าอ่านโค้ดแล้วงง ให้ลองอ่านออกเสียงเป็นภาษาคนทีละบรรทัด
  วิธีนี้ช่วยได้มากกว่าที่คิด และใช้ได้กับทุกภาษาโปรแกรม
</Callout>

<Callout type="warning">
  อย่ารัน `rm -rf` โดยไม่อ่านสิ่งที่พิมพ์ไปแล้วสองรอบ — คำสั่งนี้ไม่มีถังขยะให้กู้คืน
</Callout>

<Callout type="history">
  หน่วย "byte" ที่แปลว่า 8 บิต เพิ่งมาเป็นมาตรฐานตอน IBM System/360 ในปี 1964
  ก่อนหน้านั้นเครื่องแต่ละรุ่นนิยาม byte ไม่เท่ากัน บางเครื่องใช้ 6 บิต บางเครื่อง 9 บิต
</Callout>

<Callout type="deepdive">
  กล่องนี้ใช้ `<details>` ของ HTML จึงกดเปิดปิดได้แม้ JavaScript จะโหลดไม่สำเร็จ

  ลองกด Tab มาที่หัวเรื่องแล้วกด Enter หรือ Space ดู — ต้องเปิดปิดได้และเห็น focus ring
</Callout>

<Callout type="deepdive" title="กล่องที่เปิดค้างไว้ตั้งแต่แรก" open>
  ทดสอบ prop `open`
</Callout>

## `<Diagram>`

โหลด Mermaid เฉพาะหน้าที่มีไดอะแกรมจริง หน้าที่ไม่มีจะไม่ต้องจ่ายอะไรเลย

### Flowchart

<Diagram
  alt="ผังงานของคำสั่ง if-else เริ่มจากกล่องเริ่มต้น ไปยังคำถามว่าอายุถึง 18 ปีหรือยัง ถ้าใช่ไปที่กล่องเข้าได้ ถ้าไม่ใช่ไปที่กล่องเข้าไม่ได้ แล้วทั้งสองทางมารวมกันที่กล่องจบ"
  caption="ผังงานของ if-else ที่จะใช้ในบท 0.6"
  chart={`flowchart TD
    A([เริ่ม]) --> B{อายุ >= 18?}
    B -->|ใช่| C[เข้าได้]
    B -->|ไม่ใช่| D[เข้าไม่ได้]
    C --> E([จบ])
    D --> E`}
/>

### Sequence diagram

<Diagram
  alt="แผนภาพลำดับแสดงการทำงานของ input ผู้เรียนพิมพ์ข้อความลงช่องอินพุตก่อน จากนั้นโปรแกรมเรียก input แล้วอ่านค่าจาก buffer ทีละบรรทัด และคืนค่ากลับไปให้โปรแกรม"
  caption="วิธีที่ PythonRunner จัดการ input() โดยไม่ใช้ SharedArrayBuffer"
  chart={`sequenceDiagram
    participant U as ผู้เรียน
    participant B as ช่องอินพุต
    participant P as โปรแกรม Python
    U->>B: พิมพ์ข้อความไว้ล่วงหน้า
    P->>B: เรียก input()
    B-->>P: คืนค่าทีละบรรทัด
    P->>P: ทำงานต่อ`}
/>

## `<PythonRunner>`

Pyodide โหลดตอนกด "รัน" ครั้งแรกเท่านั้น ครั้งแรกจะช้าหน่อย ครั้งต่อไปเร็ว

### พื้นฐาน

<PythonRunner initialCode={`print("สวัสดี Rakthan")
print(bin(43))`} />

### `input()` อ่านจากช่องอินพุต

ไม่ใช้ popup และไม่ใช้ SharedArrayBuffer — กรอกไว้ล่วงหน้าหนึ่งบรรทัดต่อการเรียกหนึ่งครั้ง

<PythonRunner
  initialCode={`name = input("ชื่อ: ")
age = input("อายุ: ")
print(f"สวัสดี {name} อายุ {age} ปี")`}
  stdin={'สมชาย\n17'}
/>

### ตรวจคำตอบเองได้ด้วย `expectedOutput`

<PythonRunner
  initialCode={`# แก้ให้พิมพ์เลข 43 ในฐานสอง
print(???)`}
  expectedOutput="0b101011"
/>

### อินพุตไม่พอ — ต้องได้ EOFError ที่อ่านรู้เรื่อง

<PythonRunner
  initialCode={`a = input()
b = input()
print(a, b)`}
  stdin={'มีบรรทัดเดียว'}
/>

### ลูปไม่รู้จบ — ต้องโดนหยุดที่ 3 วินาที ไม่ใช่ค้างทั้งหน้า

<PythonRunner
  initialCode={`while True:
    pass`}
  timeout={3000}
/>

### โค้ดที่แก้ไม่ได้

<PythonRunner initialCode={`print(2 ** 10)`} editable={false} />

## `<Quiz>`

ตัวลวงทุกตัวต้องอธิบายว่าคิดผิดตรงไหน ไม่ใช่แค่บอกว่าผิด — ตอบผิดแล้วลองใหม่ได้ไม่จำกัด
ผลถูกเก็บใน localStorage กลับมาอ่านซ้ำจะเห็นว่าเคยตอบถูกแล้ว

<Quiz
  question="บิต 6 ตัว แทนค่าได้กี่แบบ?"
  options={[
    { text: '12', feedback: 'นี่คือ 6×2 — ลองทบทวนว่าเพิ่มบิตทีละตัวแล้วจำนวนแบบคูณหรือบวก' },
    { text: '36', feedback: 'นี่คือ 6² — สังเกตว่าฐานกับเลขชี้กำลังสลับที่กัน ควรเป็น 2⁶ ไม่ใช่ 6²' },
    { text: '64', correct: true, feedback: 'ถูกต้อง — 2⁶ = 64 เพราะแต่ละบิตเพิ่มทางเลือกเป็นสองเท่า' },
    { text: '128', feedback: 'นี่คือ 2⁷ ซึ่งเป็นของบิต 7 ตัว ลองนับจำนวนบิตในโจทย์อีกครั้ง' },
  ]}
/>

<Quiz
  question="ทำไมคอมพิวเตอร์ถึงใช้เลขฐาน 2 ไม่ใช่ฐาน 10?"
  options={[
    { text: 'เพราะฐาน 2 คำนวณเร็วกว่า', feedback: 'ความเร็วไม่ใช่เหตุผลตั้งต้น — ลองคิดถึงอุปกรณ์ที่ใช้เก็บค่า ว่ามันแยกสถานะได้กี่แบบ' },
    { text: 'เพราะสวิตช์ไฟฟ้ามีแค่เปิดกับปิด', correct: true, feedback: 'ถูกต้อง — วงจรแยก "มีไฟ" กับ "ไม่มีไฟ" ได้อย่างมั่นใจ แต่ถ้าต้องแยกระดับแรงดัน 10 ระดับจะพลาดง่ายมาก' },
    { text: 'เพราะมนุษย์กำหนดมาตรฐานไว้แบบนั้น', feedback: 'มาตรฐานตามมาทีหลัง คำถามคือทำไมถึงเลือกแบบนี้ตั้งแต่แรก — เหตุผลอยู่ที่ข้อจำกัดของฮาร์ดแวร์' },
  ]}
/>

## `<BinaryConverter>`

แก้ช่องไหนอีกสองช่องเปลี่ยนตาม กดที่บิตเพื่อสลับ 0/1 ได้ทั้งเมาส์และคีย์บอร์ด

<BinaryConverter initialValue={43} />

ลองพิมพ์ 300 ในช่องฐาน 10 ดู — 8 บิตเก็บไม่ได้ และตัวแปลงจะบอกว่าทำไม

### 4 บิต

<BinaryConverter initialValue={11} bits={4} />

## Component ที่ยังไม่ได้สร้าง

`<LogicGate>`, `<SqlRunner>`, `<ProgressTracker>`
