# CONTRIBUTING.md — กติกาการเขียนเนื้อหา RAKTHAN

> อ่านไฟล์นี้ก่อนเขียนหรือแก้ไขบทเรียนทุกครั้ง
> ก่อนตัดสินใจว่าจะเขียนบทอะไรและลำดับไหน อ่าน `CURRICULUM.md`
> ข้อกำหนดทางเทคนิคของ component อยู่ใน `CLAUDE.md`

---

## 1. หลักการเขียน 6 ข้อ

### 1.1 ปัญหาก่อนคำนิยาม
เปิดบทด้วยสถานการณ์ที่ผู้เรียนเห็นภาพ แล้วให้แนวคิดโผล่มาเป็น**คำตอบ**ของปัญหานั้น

❌ "Pointer คือตัวแปรที่เก็บที่อยู่ของหน่วยความจำ"
✅ "สมมติคุณต้องส่งหนังสือ 500 หน้าให้เพื่อน คุณจะถ่ายสำเนาทั้งเล่มส่งไป หรือส่งแค่ที่อยู่ห้องสมุด?"

คำนิยามที่เป็นทางการให้ตามมา**ทีหลัง** เมื่อผู้เรียนรู้แล้วว่ามันแก้ปัญหาอะไร

### 1.2 เชื่อมกับสิ่งที่ผู้เรียนรู้อยู่แล้ว
ก่อนสอนของใหม่ ให้รื้อของเก่าที่เขารู้แล้วมาดูก่อน แล้วชี้ให้เห็นว่ามันเป็นเรื่องเดียวกัน

ตัวอย่างในบท 0.2: สอนเลขฐาน 2 โดยรื้อเลขฐาน 10 ให้ดูก่อนว่า `472` แปลว่าอะไร
ผลคือผู้เรียนรู้สึกว่า "อ๋อ เรื่องเดียวกัน" ไม่ใช่ "เจอของใหม่ที่แปลกประหลาด"

### 1.3 ดักความเข้าใจผิดเสมอ
ทุกบท**ต้องมี** `<Callout type="misconception">` อย่างน้อย 1 กล่อง (CI ตรวจข้อนี้)

วิธีหาว่าควรดักอะไร: ถามตัวเองว่า "ถ้าผู้เรียนอ่านบทนี้แบบผิวเผิน เขาจะสรุปผิดว่าอะไร"
ความเข้าใจผิดที่ไม่ถูกดักตรงนี้ จะไปพังเอาตอนเรียนบทที่ลึกกว่าอีก 2 ปีข้างหน้า

### 1.4 ทุกบทต้องมีของให้ลอง
ไม่มีบทไหนเป็นทฤษฎีล้วน

**"ของให้ลอง" ไม่จำเป็นต้องเป็นโค้ด** — แต่ละแกนวิชามีรูปแบบของตัวเอง:

| แกน | รูปแบบที่ใช้ได้ |
|---|---|
| PROG | `<PythonRunner>` / `<SqlRunner>` โค้ดที่แก้แล้วเห็นผลทันที |
| MATH | `<PythonRunner>` ที่คำนวณให้เห็นภาพ หรือเครื่องมือปรับค่าแล้วเห็นกราฟเปลี่ยน |
| HARD | `<LogicGate>` / `<BinaryConverter>` / SVG ที่กดสลับสถานะได้ |
| DATA | ชุดข้อมูลจริงเล็ก ๆ ที่ query หรือ plot ได้ |
| NET | ตัวจำลองการเดินทางของ packet หรือคำนวณ subnet ที่กรอกแล้วเห็นผล |
| PROF | **กรณีศึกษาที่ต้องตัดสินใจแล้วเห็นผลลัพธ์ของแต่ละทางเลือก**, diff ให้ review หาข้อผิดพลาด, หรือ checklist ที่กรอกได้ |

**ห้ามยัดโค้ดปลอม ๆ เข้าไปเพื่อให้ผ่านกฎข้อนี้** — บทจริยธรรมวิศวกรที่มี `print("hello")` แปะไว้ท้ายบท แย่กว่าบทที่มีกรณีศึกษาดี ๆ อันเดียว

### 1.5 ทิ้ง hook ไว้เสมอ
แบบฝึกหัดข้อท้าย ๆ ให้เปิดประตูไปหาเรื่องที่ยังไม่ได้สอน โดยแขวนไว้กับเรื่องที่น่าสนใจ

ตัวอย่างในบท 0.2: ถามเรื่อง overflow โดยผูกกับเหตุจรวด Ariane 5 ระเบิดปี 1996

### 1.6 ทวนของเก่าแบบเว้นระยะ ⭐ (ข้อใหม่)

**นี่คือข้อที่กำหนดว่าผู้เรียนจะจำอะไรได้บ้างเมื่อจบคอร์ส**

หลักสูตรนี้กินเวลาเรียนเป็นปี ตัวทำนายว่าผู้เรียนจะจำเนื้อหาได้ไหม **ไม่ใช่ความดีของบทเดี่ยว ๆ แต่คือการถูกดึงความจำกลับมาใช้ซ้ำแบบเว้นระยะ**

ทุกบทต้องเปิดด้วยหัวข้อ `## ก่อนเริ่ม — ทวนของเก่า` ที่มี `<Quiz>` 2–3 ข้อ

**กติกาการเลือกคำถาม:**
- ดึงจากบทที่เรียนไปแล้ว **3–8 บทก่อนหน้า** — ไม่ใช่บทที่แล้ว
- เลือกเฉพาะแนวคิดที่บทนี้จะใช้ต่อ หรือแนวคิดที่คนลืมบ่อย
- ห้ามถามซ้ำคำถามเดิมกับที่เคยถามในบทต้นทาง — เปลี่ยนมุมถาม

ทำไมต้อง 3–8 บทก่อนหน้า? เพราะถ้าถามเรื่องบทที่แล้ว ผู้เรียนยังจำได้อยู่ การดึงกลับมาจึงไม่ต้องออกแรง และไม่ช่วยความจำระยะยาว **ความยากพอดี ๆ ในการนึกออกคือสิ่งที่ทำให้จำได้**

**ข้อยกเว้น:** บทช่วงต้นที่ยังไม่มีของเก่าพอให้ทวน (บท 0.1–0.4) ให้ทวนเท่าที่มี — ตารางที่ใช้จริงอยู่ใน `CURRICULUM.md` §2

---

## 2. โครงบทมาตรฐาน (ห้ามสลับลำดับ)

```
 1. Frontmatter
 2. # ชื่อบท
 3. ## จบบทนี้แล้วคุณจะ...      ← bullet 3-5 ข้อ ขึ้นต้นด้วยกริยาที่วัดผลได้
 4. ## ก่อนเริ่ม — ทวนของเก่า    ← <Quiz> 2-3 ข้อ จากบท 3-8 บทก่อนหน้า
 5. ## ทำไมต้องมีสิ่งนี้         ← เรื่องเล่า/ปัญหา ไม่เกิน 250 คำ
 6. ## แนวคิดหลัก               ← แบ่งเป็นหัวข้อย่อยเลข 1, 2, 3
 7. ## ลงมือทำ                  ← บันได 3 ขั้น (ดูหัวข้อ 4.2)
 8. ## แบบฝึกหัด                ← ง่าย 3 / กลาง 3-4 / ท้าทาย 2-3
 9. ## เช็คก่อนไปต่อ             ← 3 คำถาม + บอกว่าถ้าตอบไม่ได้ให้กลับไปอ่านหัวข้อไหน
10. ## เชื่อมโยงต่อ              ← ย้อนไปบทไหน / จะกลับมาอีกทีบทไหน
11. ## อ่านเพิ่ม
```

### หัวข้อ 9 "เช็คก่อนไปต่อ" — ทำไมต้องมี

`prerequisites` ใน frontmatter บอกว่า *ต้องเรียนบทไหนมาก่อน* แต่ไม่มีอะไรบอกว่า *เรียนแล้วเข้าใจพอหรือยัง* และผู้เรียนมือใหม่ตัดสินเรื่องนี้เองไม่ได้ — คนที่เข้าใจน้อยที่สุดมักมั่นใจที่สุด

รูปแบบ:

```mdx
## เช็คก่อนไปต่อ

ถ้าตอบสามข้อนี้ไม่ได้ อย่าเพิ่งไปบทถัดไป

1. ทำไมคอมพิวเตอร์ถึงใช้ฐาน 2 ไม่ใช่ฐาน 10? → ถ้าตอบไม่ได้ กลับไปอ่าน "แนวคิดหลัก ข้อ 1"
2. เลข 200 เก็บใน 1 ไบต์ได้ไหม เพราะอะไร? → กลับไปอ่าน "แนวคิดหลัก ข้อ 3"
3. ...
```

**ต้องชี้หัวข้อที่เจาะจง** ไม่ใช่ "กลับไปอ่านบทนี้อีกรอบ" ซึ่งช่วยอะไรไม่ได้

---

## 3. ภาษาและศัพท์

### 3.1 กฎการใช้ศัพท์เทคนิค
ครั้งแรกที่เจอ: **ไทย (อังกฤษ)** เช่น "หน่วยความจำ (memory)"
ครั้งต่อไป: ใช้ทับศัพท์อังกฤษได้เลย

**เหตุผล:** ผู้เรียนต้องอ่าน documentation ภาษาอังกฤษเป็นในที่สุด ถ้าเราแปลทุกอย่างเป็นไทยหมด เขาจะเปิด Stack Overflow ไม่รู้เรื่อง

#### "ครั้งแรก" หมายถึงครั้งแรก**ของบทนี้** ไม่ใช่ของทั้งคอร์ส

ผู้เรียนไม่ได้อ่านเรียงจากบท 0.1 เสมอไป — คนที่ค้น Google เจอบท 2.14 แล้วกระโดดเข้ามาตรง ๆ จะเจอศัพท์ที่ "อธิบายไปแล้วเมื่อบท 0.7" ซึ่งเขาไม่เคยเห็น

ดังนั้น **ทุกบทตั้งต้นใหม่เสมอ** ศัพท์เทคนิคที่ปรากฏครั้งแรกในบทนั้น ต้องมีวงเล็บภาษาอังกฤษกำกับ ต่อให้เคยอธิบายในบทก่อนหน้าแล้วก็ตาม

#### Glossary กลาง

ศัพท์ที่อธิบายเต็ม ๆ ต้องมีรายการใน `docs/appendix/glossary.mdx` และครั้งแรกในบทควรลิงก์ไป:

```mdx
หน่วยความจำ ([memory](/docs/appendix/glossary#memory)) คือ...
```

Glossary คือที่เดียวที่คำนิยามเป็นทางการอยู่ — บทเรียนอธิบายด้วยปัญหาและตัวอย่าง ไม่ใช่ด้วยคำนิยาม (ดูข้อ 1.1)

### 3.2 ศัพท์ที่ห้ามแปลไทย
`pointer`, `stack`, `heap`, `cache`, `thread`, `process`, `commit`, `array`, `hash`, `query`, `token`, `gradient` — ใช้ทับศัพท์ตรง ๆ เพราะคำแปลไทยไม่เป็นที่ใช้จริงในวงการ

### 3.3 ศัพท์ที่ควรใช้ไทย
หน่วยความจำ, ตัวแปร, ฟังก์ชัน, เงื่อนไข, การวนซ้ำ, ชนิดข้อมูล, ฐานข้อมูล, เครือข่าย

### 3.4 โทนเสียง
- เรียกผู้เรียนว่า "คุณ" ไม่ใช่ "นักเรียน" หรือ "ผู้เรียน"
- ใช้ "เรา" เมื่อชวนคิดร่วมกัน: "ลองมาดูกันว่า..."
- ห้ามใช้คำว่า "ง่ายมาก" "แค่นี้เอง" "ชัดเจนอยู่แล้ว" — ถ้ามันง่ายจริงผู้เรียนคงไม่ต้องมาอ่าน คำพวกนี้ทำให้คนที่ไม่เข้าใจรู้สึกโง่และเลิกเรียน (CI ตรวจข้อนี้)
- ห้ามใช้อีโมจิในเนื้อหา (ยกเว้นในตารางเช็คลิสต์)

### 3.5 ความยาว

| ระดับ | เพดาน |
|---|---|
| ย่อหน้า | 4 บรรทัด |
| หัวข้อย่อย | 400 คำ ถ้าเกินให้แตกหัวข้อ |
| **ทั้งบท** | **`duration` × 25 ถึง `duration` × 42 คำไทย** ไม่นับโค้ดและ frontmatter |

ตัวอย่าง: บท 60 นาที → 1,500–2,520 คำ · บท 90 นาที → 2,250–3,780 คำ · บท 120 นาที → 3,000–5,040 คำ

**ทำไมต้องนับคำ ไม่ใช่นับนาที:** "อ่าน+ทำได้ใน 60–120 นาที" คนเขียนวัดไม่ได้ตอนกำลังเขียน จะรู้ตัวก็ตอนบทยาว 8,000 คำไปแล้ว งบคำนับได้ทันทีและ CI ตรวจให้

**ทำไมงบถึงผูกกับ `duration`:** ของเดิมตั้งไว้ 1,800–3,000 คำเท่ากันทุกบท ซึ่งใช้ไม่ได้ เพราะ `duration` ต่างกันได้เท่าตัว บทสั้นที่เขียนกระชับดีอยู่แล้วจะโดนเตือนโดยไม่มีเหตุผล แล้วคนเขียนจะเติมน้ำให้ผ่านเกณฑ์ — ซึ่งแย่กว่าปล่อยให้สั้น

ถ้าเกินกรอบบน **ให้แยกเป็น 2 บท** อย่าบีบเนื้อหาให้สั้นลงจนอธิบายไม่ครบ

---

## 4. โค้ดตัวอย่างและการฝึก

### 4.1 กฎของโค้ด
1. **ต้องรันทดสอบจริงก่อนใส่ในบท** ทุกชิ้น ไม่มีข้อยกเว้น
2. เก็บไฟล์จริงไว้ที่ `code/stage-N/lesson-XX/` และให้บทเรียนอ้างอิง
3. คอมเมนต์ในโค้ดเป็นภาษาไทย
4. ชื่อตัวแปรเป็นภาษาอังกฤษเสมอ (ห้ามใช้ตัวแปรภาษาไทยแม้ Python จะรองรับ)
5. ยาวไม่เกิน 25 บรรทัดต่อบล็อก ถ้ายาวกว่านั้นให้แบ่งอธิบายทีละส่วน
6. ใส่ output ที่คาดหวังเป็นคอมเมนต์: `print(bin(43))  # 0b101011`

### 4.2 บันได 3 ขั้นในหัวข้อ "ลงมือทำ" ⭐ (ข้อใหม่)

**ห้ามกระโดดจากตัวอย่างที่อธิบายเสร็จ ไปเป็นโจทย์เปล่าทันที** — รอยต่อนี้คือจุดที่ผู้เรียนที่ยังไม่มั่นใจหลุดออกไป

ทุกบทที่มีโค้ดต้องไล่ 3 ขั้นตามลำดับ:

**ขั้น 1 — ดูของที่ทำเสร็จแล้ว**
โค้ดสมบูรณ์ที่รันได้ พร้อมอธิบายว่าแต่ละส่วนทำอะไร ผู้เรียนแค่กดรันแล้วสังเกตผล

```mdx
<PythonRunner initialCode={`# แปลงเลขฐาน 10 เป็นฐาน 2
number = 43
print(bin(number))   # 0b101011`} />
```

**ขั้น 2 — เติมช่องว่าง**
โค้ดเดิมที่เจาะออก 1–2 จุด โครงสร้างยังอยู่ครบ ผู้เรียนเติมแค่ส่วนที่เป็นแก่นของแนวคิด

```mdx
<PythonRunner initialCode={`# เติมให้แปลงเป็นฐาน 16 แทน
number = 43
print(____(number))   # ควรได้ 0x2b`} />
```

**ขั้น 3 — โจทย์เปล่า**
ค่อยไปหัวข้อ "แบบฝึกหัด"

**เหตุผล:** สำหรับมือใหม่ การเห็นตัวอย่างที่ทำเสร็จแล้วช่วยได้มากกว่าการลองแก้โจทย์เอง (worked example effect) และการเติมช่องว่างคือสะพานระหว่างสองอย่าง ถ้าข้ามขั้น 2 ผู้เรียนจะเจอโจทย์เปล่าโดยยังไม่ได้ลองขยับอะไรเลย

### 4.3 แสดงโค้ดที่ผิดก่อนโค้ดที่ถูก
เมื่อสอนเรื่องที่คนพลาดบ่อย ให้แสดงโค้ดที่ผิดพร้อม error จริง แล้วค่อยแก้ให้ดู
ผู้เรียนจะจำ error message ได้และแก้เองเป็นในอนาคต

**ต้องเป็น error message จริงที่ copy มาจากการรันจริง** ห้ามพิมพ์ขึ้นเองจากความทรงจำ

---

## 5. ไดอะแกรมและภาพ

### เมื่อไหร่ที่ **ต้อง** มีไดอะแกรม

| สถานการณ์ | บังคับ |
|---|---|
| อธิบายโครงสร้างที่มีส่วนประกอบตั้งแต่ 3 ส่วนขึ้นไปและมีความสัมพันธ์กัน | ✅ |
| อธิบายลำดับขั้นตอนที่มีการแตกกิ่ง | ✅ |
| อธิบายการจัดวางในหน่วยความจำ / โครงสร้างข้อมูล | ✅ |
| อธิบายวงจร สัญญาณ หรือสถาปัตยกรรม (แกน HARD, NET) | ✅ |
| นิยามหรือข้อเท็จจริงเดี่ยว ๆ | ไม่ต้อง |

**เหตุผล:** เมื่อคำอธิบายเป็นเรื่องเชิงพื้นที่หรือเชิงโครงสร้าง การเขียนเป็นข้อความล้วนบังคับให้ผู้เรียนสร้างภาพในหัวเอง ซึ่งกินกำลังสมองที่ควรเอาไปใช้ทำความเข้าใจแทน — และภาษาไทยที่เป็นกำแพงข้อความยาว ๆ อ่านยากกว่าอังกฤษด้วยเพราะไม่มีช่องว่างระหว่างคำ

### กติกา
- Flowchart / ลำดับขั้น → ใช้ **`<Diagram>`** (Mermaid)

  ```mdx
  import Diagram from '@site/src/components/Diagram';

  <Diagram
    alt="ผังงานของ if-else เริ่มจากกล่องเริ่มต้น ไปยังคำถามว่าอายุถึง 18 หรือยัง ..."
    caption="ผังงานของ if-else"
    chart={`flowchart TD
      A([เริ่ม]) --> B{อายุ >= 18?}
      B -->|ใช่| C[เข้าได้]
      B -->|ไม่ใช่| D[เข้าไม่ได้]`}
  />
  ```

  **ห้ามใช้ ` ```mermaid ` fence** — โปรเจกต์นี้ไม่ได้ติดตั้ง `@docusaurus/theme-mermaid`
  เพราะมันทำให้ทุกหน้าต้องโหลด JS เพิ่ม 141 KB แม้หน้าที่ไม่มีไดอะแกรม (ดู `CLAUDE.md` หัวข้อ 9)
  `<Diagram>` โหลด mermaid เฉพาะหน้าที่ใช้จริง

  prop `alt` เป็น**ฟิลด์บังคับในระดับ TypeScript** เขียนบทโดยลืมใส่แล้ว build จะพังทันที

- วงจร / สถาปัตยกรรม / การจัดวางหน่วยความจำ → **SVG เขียนเอง** เก็บใน `static/img/diagrams/`
- **สีในไดอะแกรมต้องใช้ CSS variable** เพื่อให้เปลี่ยนตามโหมดสว่าง/มืด — SVG ที่ hardcode `#000000` จะหายไปในโหมดมืด
- ทุกภาพต้องมี `alt` ภาษาไทยที่**บรรยายสิ่งที่ภาพสื่อ** ไม่ใช่ชื่อภาพ
  - ❌ `alt="ไดอะแกรมหน่วยความจำ"`
  - ✅ `alt="แถวของช่องหน่วยความจำ 8 ช่อง แต่ละช่องมีที่อยู่กำกับตั้งแต่ 0x00 ถึง 0x07 ช่องที่ 3 เก็บค่า 42"`
- ห้ามสื่อความหมายด้วยสีอย่างเดียว ต้องมีป้ายกำกับหรือลวดลายด้วย

---

## 6. แบบฝึกหัดและเฉลย

| ระดับ | จำนวน | ลักษณะ |
|---|---|---|
| ง่าย | 3 ข้อ | ใช้สูตร/วิธีในบทตรง ๆ **ต้องตรวจคำตอบเองได้ทันที** |
| กลาง | 3–4 ข้อ | ต้องประยุกต์ หรือรวมสองแนวคิดเข้าด้วยกัน |
| ท้าทาย | 2–3 ข้อ | เปิดปลาย / ต้องไปค้นเพิ่ม / เชื่อมกับบทอนาคต |

### ระดับ "ง่าย" ต้องตรวจเองได้ — และต้องบอกว่าตรวจยังไง

"ตรวจคำตอบเองได้" ไม่ใช่คำแนะนำลอย ๆ ต้องมีกลไกจริง เลือกอย่างใดอย่างหนึ่ง:
- `<PythonRunner expectedOutput="...">` — ผู้เรียนรันแล้วรู้เองว่าถูกไหม
- `<Quiz>` ที่มีตัวเลือก
- ระบุผลลัพธ์ที่ควรได้ไว้ในโจทย์ เช่น "ควรได้ 0b101011"

### เฉลย — กติกาที่เข้มขึ้น ⭐

**ห้ามใส่เฉลยในไฟล์บทเรียน** ให้แยกไว้ที่ `docs/appendix/solutions/{slug}.mdx` และลิงก์ไป

แต่: **ต้องเขียนไฟล์เฉลยพร้อมกับบท ในงานเดียวกัน ไม่ใช่ตามมาทีหลัง**

เหตุผล: กลุ่มเป้าหมายคือคนเรียนเอง ไม่มีครู ไม่มี TA ถ้าไฟล์เฉลยไม่มี ผู้เรียนที่ติดจะติดถาวรและเลิกเรียน — และ "เดี๋ยวมาเขียนทีหลัง" แปลว่าไม่มีวันได้เขียน

`scripts/validate-frontmatter.mjs` จะ **fail build** ถ้าบทที่ `status: published` ไม่มีไฟล์เฉลยคู่กัน

เฉลยต้องอธิบาย **วิธีคิด** ไม่ใช่แค่คำตอบ — และสำหรับข้อที่มีวิธีทำหลายทาง ให้แสดงมากกว่าหนึ่งทาง

---

## 7. Component ที่ใช้ในบทเรียน

```mdx
import Callout from '@site/src/components/Callout';
import PythonRunner from '@site/src/components/PythonRunner';
import Quiz from '@site/src/components/Quiz';
```

| ใช้เมื่อ | Component |
|---|---|
| ดักความเข้าใจผิด (บังคับ ≥1 ต่อบท) | `<Callout type="misconception">` |
| เกร็ดประวัติศาสตร์ | `<Callout type="history">` |
| เนื้อหาลึกเกินระดับ (พับได้) | `<Callout type="deepdive">` |
| เตือนอันตราย/ข้อผิดพลาดร้ายแรง | `<Callout type="warning">` |
| ข้อสังเกตทั่วไป | `<Callout type="note">` |
| เคล็ดลับที่ช่วยให้ทำงานง่ายขึ้น | `<Callout type="tip">` |
| ให้ลองรันโค้ด Python | `<PythonRunner>` |
| ทวนของเก่าต้นบท / เช็คความเข้าใจกลางบท | `<Quiz>` |
| ผังงาน / แผนภาพลำดับ | `<Diagram>` (ดูหัวข้อ 5) |

### ตำแหน่งของ `<Quiz>` — ใช้ 3 จุด

| จุด | จำนวน | ดึงจากไหน |
|---|---|---|
| ต้นบท ("ก่อนเริ่ม") | 2–3 ข้อ | บทที่ผ่านมา 3–8 บทก่อนหน้า |
| กลางบท (ท้าย "แนวคิดหลัก" แต่ละข้อใหญ่) | 1 ข้อ | เนื้อหาที่เพิ่งอ่านจบ |
| ท้ายบท | ไม่บังคับ | ภาพรวมทั้งบท |

**ที่สำคัญที่สุดคือกลางบท** เพื่อให้ผู้เรียนรู้ตัวเร็วว่าตามไม่ทัน ไม่ใช่รู้ตอนอ่านจบแล้ว

### `<PythonRunner>` กับ `input()`

Runner ใช้ช่องอินพุตแยก ไม่ใช่ popup — ถ้าโค้ดในบทเรียก `input()` **ต้องใส่ prop `stdin` ให้ด้วยเสมอ** ไม่งั้นผู้เรียนกดรันแล้วจะเจอ error ทันที

```mdx
<PythonRunner
  initialCode={`name = input("ชื่อ: ")\nprint(f"สวัสดี {name}")`}
  stdin={'สมชาย'}
/>
```

---

## 8. เช็คลิสต์ก่อนบอกว่าบทเสร็จ

### เนื้อหา
- [ ] Frontmatter ครบทุกฟิลด์และถูกต้อง (รวม `description` และ `last_reviewed`)
- [ ] มี "จบบทนี้แล้วคุณจะ..." ที่วัดผลได้จริง
- [ ] มี "ก่อนเริ่ม — ทวนของเก่า" ที่ดึงจากบท 3–8 บทก่อนหน้า
- [ ] เปิดบทด้วยปัญหา ไม่ใช่คำนิยาม
- [ ] มี `<Callout type="misconception">` อย่างน้อย 1 กล่อง
- [ ] "ลงมือทำ" ไล่ครบบันได 3 ขั้น (ดู → เติมช่องว่าง → โจทย์เปล่า)
- [ ] มีแบบฝึกหัดครบ 3 ระดับ และระดับง่ายตรวจเองได้จริง
- [ ] **มีไฟล์เฉลยใน `docs/appendix/solutions/` แล้ว**
- [ ] มี "เช็คก่อนไปต่อ" ที่ชี้หัวข้อเจาะจง
- [ ] มีหัวข้อ "เชื่อมโยงต่อ" ที่ลิงก์ไปบทจริง
- [ ] ศัพท์เทคนิคครั้งแรก**ในบทนี้** มีวงเล็บภาษาอังกฤษ และมีรายการใน glossary
- [ ] มีไดอะแกรมในทุกจุดที่หัวข้อ 5 บังคับ และ `alt` บรรยายเนื้อหาจริง

### รูปแบบ
- [ ] ความยาว 1,800–3,000 คำ
- [ ] ไม่มีย่อหน้าไหนเกิน 4 บรรทัด
- [ ] ไม่มีคำว่า "ง่ายมาก" "แค่นี้เอง" "ชัดเจนอยู่แล้ว"
- [ ] โค้ดทุกชิ้นรันทดสอบแล้ว และเก็บไว้ใน `code/`
- [ ] error message ทุกอันคัดลอกมาจากการรันจริง

### เทคนิค
- [ ] `npm run check` ผ่าน
- [ ] `npm run build` ผ่าน
- [ ] **`npm run check:drafts` ผ่าน** — `npm run build` ข้ามไฟล์ `draft: true` ทั้งหมด
      บทที่ยังไม่เสร็จจึงมี MDX พังได้โดย build ไม่ฟ้อง

:::warning กับดัก MDX ที่เจอบ่อยที่สุด

ถ้าใส่ JSX ไว้ใน**รายการ** (`1.` `2.` `-`) ห้ามให้ template literal ขึ้นบรรทัดใหม่ที่คอลัมน์ 1

```mdx
1. โจทย์

   <PythonRunner initialCode={`print(1)
print(2)`} />        ← พัง
```

ให้เขียนเป็นสตริงบรรทัดเดียวแทน

```mdx
   <PythonRunner initialCode={"print(1)\nprint(2)"} />   ← ใช้ได้
```

:::
- [ ] เปิดดูบนความกว้าง 375px แล้วไม่ล้น
- [ ] ทดสอบทั้งโหมดสว่างและมืด

---

## 9. เมื่อไม่แน่ใจข้อเท็จจริง

ห้ามเดา ให้ใส่:
```mdx
{/* TODO: verify — ไม่แน่ใจว่าตัวเลขนี้ถูกต้อง ต้องตรวจกับแหล่งอ้างอิง */}
```
แล้ว**แจ้งผู้ใช้ในสรุปงาน** ว่ามีจุดไหนที่ต้องตรวจสอบบ้าง

เนื้อหาการศึกษาที่ผิดอันตรายกว่าไม่มีเนื้อหา เพราะผู้เรียนไม่มีทางรู้ว่ามันผิด

### สถานะการตรวจ

| status | ความหมาย |
|---|---|
| `draft` | เขียนเสร็จแล้วแต่ยังไม่มีใครตรวจ **ไม่ขึ้น production build** |
| `review` | มีคนอ่านตรวจข้อเท็จจริงแล้ว 1 รอบ แต่ยังไม่ได้ทดสอบกับผู้เรียนจริง |
| `published` | ตรวจข้อเท็จจริงแล้ว โค้ดรันได้จริง มีเฉลยครบ ผ่าน `npm run check` |

เลื่อนสถานะเป็น `published` ได้ก็ต่อเมื่อ**มีคนที่ไม่ใช่ผู้เขียน**อ่านตรวจแล้ว
ถ้าทำคนเดียว ให้ทิ้งช่วงอย่างน้อย 3 วันแล้วกลับมาอ่านใหม่ก่อนเลื่อนสถานะ — และอัปเดต `last_reviewed`
